import React from "react";
import NumberGeneratorTag from "./NumberGenerator";

export default {
    title: 'E6',
    component: NumberGeneratorTag,
}

export const NumberGenerator = (args) => <NumberGeneratorTag {...args} />;